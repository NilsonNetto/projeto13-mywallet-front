import styled from "styled-components";

export default function Transaction() {

  // fazer input de valor, de descrição e embaixo um botão de enviar, enviando para a rota

  return (
    <Wrapper>

    </Wrapper>
  );
}

const Wrapper = styled.div`

`;